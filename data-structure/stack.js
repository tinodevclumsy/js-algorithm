// A  stack is a data structure has a list of elements
// follows LIFO order (Last In, First Out)

let stack = []

stack.push(1) // [1]
stack.push(2) // [1,2]

console.log(stack);
let p = stack.pop() // 2

console.log(p)